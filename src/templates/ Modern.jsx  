const Modern = ({ data }) => {
  return (
    <div className="flex min-h-[297mm] bg-white shadow-lg text-gray-800">
      {/* Sidebar - Aksen Warna */}
      <div className="w-1/3 bg-slate-800 text-white p-8">
        <div className="mb-10">
          <h1 className="text-2xl font-bold leading-tight uppercase">{data.name}</h1>
          <p className="text-slate-300 text-sm">{data.role}</p>
        </div>

        <div className="mb-6">
          <h3 className="text-xs font-bold uppercase tracking-widest text-slate-400 mb-2">Kontak</h3>
          <p className="text-xs">{data.email}</p>
        </div>

        <div>
          <h3 className="text-xs font-bold uppercase tracking-widest text-slate-400 mb-2">Skills</h3>
          <div className="flex flex-wrap gap-2">
            {data.skills?.map((skill, i) => (
              <span key={i} className="text-[10px] bg-slate-700 px-2 py-1 rounded">{skill}</span>
            ))}
          </div>
        </div>
      </div>

      {/* Main Content */}
      <div className="w-2/3 p-10">
        <section className="mb-8">
          <h2 className="text-lg font-bold text-slate-800 border-b-2 border-slate-800 mb-3">Tentang Saya</h2>
          <p className="text-sm leading-relaxed">{data.summary}</p>
        </section>

        <section>
          <h2 className="text-lg font-bold text-slate-800 border-b-2 border-slate-800 mb-3">Pengalaman Kerja</h2>
          {data.experience?.map((exp, i) => (
            <div key={i} className="mb-4">
              <h4 className="font-bold text-sm">{exp.company}</h4>
              <p className="text-xs text-gray-500 font-medium">{exp.year}</p>
              <p className="text-xs mt-1 text-gray-700">{exp.desc}</p>
            </div>
          ))}
        </section>
      </div>
    </div>
  );
};

export default Modern;